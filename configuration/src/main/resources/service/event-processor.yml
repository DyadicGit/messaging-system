spring:
  cloud:
    stream:
      bindings:
        input:
          destination: http-message
          group: http-messages
      kafka:
        binder:
          zkNodes: localhost:2181
          brokers: localhost:9092


server:
  port: 8100
logging:
  level:
    org:
      cynic:
        messaging:
          consumer:
            service: INFO